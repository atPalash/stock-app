from setuptools import setup, find_packages

setup(
    name="pytick",
    version="0.1.0",
    description="Package for stock data handling and analysis",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],  # Add your dependencies here if needed
)